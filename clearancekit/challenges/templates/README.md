# Turnstile Templates (legacy)

Template-based matching (`AutoClickSolver` + opencv + mss) has been replaced
by `CDPClickSolver` and `OSClickSolver`, which locate the challenge element
via CDP DOM queries instead of screenshot matching.

This directory is kept for historical reference. No PNG templates are needed
for the current solver pipeline.
